# 许可证与商业授权

本项目采用**双重许可（dual licensing）**模式。

## 社区版：AGPL-3.0

本仓库源码在 **GNU Affero General Public License v3.0**（见 `LICENSE`）下授权。要点：

- **强传染性（copyleft）**：任何分发或基于本项目的衍生作品，必须以 AGPL-3.0 开源完整对应源码。
- **网络服务条款（第 13 条）**：通过网络向用户提供修改版服务（如 SaaS、Web 服务）时，同样必须向这些用户提供完整源码；不分发二进制也不能免除该义务。
- **允许商用**：AGPL 本身不禁止商业使用；但商用者若不愿开源其改动，就必须走下面的商业授权。

## 商业版：专有授权

需要以下任一情形的，请获取**商业授权**，即可在**不开源**的前提下使用与修改本项目：

- 将本项目（含修改）集成进闭源产品对外发布；
- 提供不希望公开源码的网络服务；
- 不希望受 AGPL 传染性约束的任何商用场景。

商业授权联系：邮件至 **3308786104@qq.com**（或通过仓库 Issue），注明主体、使用场景（内部使用 / 闭源集成 / SaaS）与期望版本，洽谈授权范围、期限与费用。

- 协议模板：`docs/COMMERCIAL-LICENSE.md`（含 A 内部使用 / B 闭源集成再分发 / C 网络服务三档授权与授权证书模板）；
- 甲方每次发放授权时复制模板为独立合同文本填写签署，并在授权台账登记授权编号（规则见模板附录一）。

## 贡献者许可协议（CLA）

为长期维持双重授权的能力（即维护者保留将代码另行授权给商业客户的权利），本项目要求所有贡献者签署 CLA。

- 协议正文：`docs/CLA.md`（贡献者保留版权，授予维护者永久、不可撤销、可再许可含闭源商用的版权与专利许可）。
- 贡献指南：`CONTRIBUTING.md`；PR 模板含 CLA 确认项。
- 校验工具：**cla-assistant**，配置见仓库根 `.cla-assistant.json`，在 Pull Request 上自动检查签署状态。

### 启用 cla-assistant（一次性，需仓库管理员操作）

1. 安装 GitHub App：访问 https://cla-assistant.io ，用仓库管理员账号登录并授权 `chen647208/hongyue-creation` 仓库。
2. cla-assistant 会读取仓库根的 `.cla-assistant.json`，其中 `path` 指向 `docs/CLA.md`。
3. 在仓库 Settings → Branches 对 `main` 开启分支保护，并把 cla-assistant 的 `license/cla` 状态检查设为必需（Required check），未签署 CLA 的 PR 即无法合并。
4. 建议创建两个标签 `cla: yes` / `cla: no`（配置已引用）。

> 若不想引入外部 App，可退化为「PR 模板勾选 + 人工核对」的轻量方式，但自动化校验更可靠、推荐用 cla-assistant。

## 术语澄清

- AGPL-3.0 是 **OSI 认证的开源许可证**，本项目可正当称为"开源"。
- 若未来希望"未授权方连商用都禁止"，那属于**非商用的源码可用（source-available）**许可，不再是 OSI 开源，需单独评估——当前方案通过"copyleft + 商业授权"已能实现变现，通常无需额外禁止商用。

## 一句话总结

> 免费用可以，但改动要开源（含 SaaS）；想闭源商用，请买商业授权。
